# international 프로젝트 계획

## 처음 기획 의도

한국 언론을 반복 수집하는 대신, 해외 정부·국제기구·연구기관이 한국, 북한,
한반도와 한국 기업을 직접 언급한 공식 원문을 조기에 발견해 텔레그램으로
알리는 모니터를 운영한다.

이 프로젝트는 다음 목적을 계속 지킨다.

- 해외의 1차 공식 발표와 전문 분석을 우선한다.
- 한국 언론에서 이미 확인할 수 있는 보도나 한국 언론을 재인용한 보도는
  알림 대상에서 제외한다.
- 특정 국가나 영어권에 치우치지 않고 여러 국가와 언어의 원문을 다룬다.
- 새 소스는 첫 성공 실행에서 기준선만 저장한 뒤 후속 신규·수정 항목부터
  알린다.
- 운영 상태는 GitHub Actions와 저장소의 `data/` 기록을 기준으로 판단한다.

## 진행 기록

### 2026-07-25: 작업폴더 복원과 다국가·다국어 확장 시작

- 빈 작업폴더에 `coolpint/international`의 최신 `main`을 체크아웃했다.
- 원격 최신 커밋 `a50c60e`와 일치하는 것을 확인했다.
- GitHub 인증을 복구하고 `agent/expand-global-sources` 브랜치를 만들었다.
- 사용자 요청을 다음 세 작업으로 구체화했다.
  1. 해외 공식 소스를 여러 국가와 언어로 확대한다.
  2. 한국 언론을 출처로 삼거나 재인용한 항목을 제외한다.
  3. 한국 기업명과 주요 외국어 표기를 확대한다.
- 소스 후보는 실제 응답과 자동수집 형식을 확인한 뒤 채택하기로 했다.
- 기존 원격 설정에 남아 있는 ILO 404와 CISA 403 소스도 함께 복구하기로
  했다. 깨진 기존 소스를 둔 채 새 소스만 추가하면 전체 운영 품질을
  설명할 수 없기 때문이다.
- constitution 에이전트는 공식 Atom/RSS 우선, 중국·호주 HTML 소스의
  조건부 채택, 명확한 한국 언론사명과 인용 표현의 동시 일치, 기업 단독
  약어 금지, 다국어 한반도 키워드 병행 확대를 요구했다.
- live 소스 검증 결과:
  - 영국 FCDO Atom, 캐나다 Global Affairs Atom, EU Council 프랑스어
    RSS, 독일 외무부 독일어 RSS가 정상 항목을 반환했다.
  - 중국 외교부 중국어 목록은 날짜별 성명 링크만 선택하도록 selector를
    제한한 뒤 정상 상세 항목을 반환했다.
  - 호주 DFAT는 목록 응답이 반복적으로 제한 시간을 초과해 활성화하지
    않고 `pending`으로 기록했다.
  - ILO 공식 All news 목록과 CISA 공식 advisory RSS 교체안이 정상
    수집됐다.
  - 기존 UNCTAD 공식 페이지는 반복적으로 Cloudflare 403을 반환해
    활성 소스에서 `pending`으로 전환했다.
- 단위 테스트 45개, JSON 구문 검사, `git diff --check`가 통과했다.
- 전체 live dry-run에서 신규 5개 소스의 bootstrap이 확인됐고 상태
  파일은 변경되지 않았다. 최초 실행의 유일한 오류였던 UNCTAD는 위
  결정에 따라 비활성화했다.
- 비활성화 반영 후 최종 live dry-run을 다시 실행해 활성 소스 전체가
  오류 없이 수집되는 것을 확인했다. 신규 5개 소스는 각각 영국 11건,
  캐나다 30건, EU 프랑스어 20건, 독일어 10건, 중국어 20건을
  수집했고, ILO 20건과 CISA 1건도 정상 수집됐다.
- 한국 기업 키워드의 모호한 단독 약어와 회사로 오인될 수 있는 일반
  명사를 제거했다. 영어와 중국어의 한국 언론 재인용 사례를 실제 설정
  기반 테스트에 포함했다.
- 1차 review에서 전문 소스의 기본 관련도가 명시적 제외 결과를 다시
  활성화하는 문제와, 서로 다른 문장의 언론사명·인용 표현을 잘못
  결합하는 문제를 발견했다. `Classification.excluded`를 하드 제외
  상태로 추가하고, 한국 언론 인용 판정 범위를 같은 문장으로 제한했다.
- 재검토에서 빠진 영어 능동 과거형(`Yonhap reported ...`)을 발견해
  인용 표현과 실제 설정 기반 회귀 사례에 추가했다. 최종 45개 테스트가
  통과했다.
- 모든 리뷰 수정 후 최종 live dry-run을 다시 실행했다. 활성 소스가
  모두 오류 없이 수집됐고 관련 항목 18건, bootstrap 이벤트 1건을
  확인했으며 dry-run 상태 파일은 변경되지 않았다.
- 최종 review는 코드·설정·문서, constitution 준수, 회귀 테스트를
  재확인하고 “발견된 문제 없음”으로 승인했다.
- 구현 커밋 `698211a`를 `agent/expand-global-sources` 브랜치에
  푸시하고 GitHub 초안 PR #1을 만들었다. 기준 브랜치는 최신
  `origin/main`의 `a50c60e`이며, 로컬과 원격 차이가 없는 상태에서
  작업 커밋을 만들었다.

## 현재 작업 단계

- [x] 원격 `main`과 작업폴더 동기화
- [x] 프로젝트 판단 기준과 계획 문서 복원
- [x] constitution 에이전트 검토
- [x] 다국가·다국어 공식 소스 추가
- [x] 한국 언론 재인용 제외 규칙 추가
- [x] 한국 기업 키워드 확대
- [x] 단위 테스트와 live dry-run
- [x] review 에이전트 검토
- [x] 커밋, 푸시, PR 생성

### 2026-08-18: NYT·WSJ 한국 뉴스 필수 알림

- 사용자 요청에 따라 뉴욕타임스와 월스트리트저널을 두 매체에만
  한정된 고우선 해외 미디어 예외로 추가하기로 했다.
- 기존 PR 브랜치를 최신 `origin/main` 위로 재배치했다. 원격 main에는
  자동 수집 커밋 71개가 추가돼 있었고, 기존 PR #1은 열린 상태였다.
- constitution 에이전트는 최신의 구체적인 “반드시 알려줘” 요청이 두
  매체에 한해 기존 한국 언론 재인용 제외보다 우선한다고 판단했다.
  다른 해외 소스에는 기존 제외 원칙을 유지한다.
- 실제 수집 경로를 비교한 결과 NYT 공식 섹션 RSS는 최신이지만 여러
  섹션으로 분산돼 있었고, WSJ 공식 RSS는 최신 항목이 2025년에 머물러
  있었다. 두 매체 모두 최신 2026년 한국 기사를 반환한 Google News의
  최근 30일 매체 제한 검색 RSS를 채택했다.
- 검색 중개 피드의 오인과 변조를 줄이기 위해 각 항목의 `<source>`
  이름과 발행처 도메인을 NYT·WSJ로 모두 검증하고, 검증 결과가 0건이면
  오류로 처리한다.
- 두 소스의 검증된 결과는 `high`로 강제하고, 한국 언론 인용 때문에
  일반 필터가 제외한 경우에도 알림을 복원하면서 `한국 언론 인용 포함`
  근거를 남기도록 구현했다.
- 첫 전체 live dry-run에서 새 NYT 99건과 WSJ 98건은 정상 수집됐지만,
  기존 ECB 피드가 로컬 Python의 CA 번들 부족으로 인증서 검증에
  실패했다. HTTPS 검증을 끄지 않고 `certifi` CA 번들을 명시적으로
  사용하도록 공통 HTTP 경로를 보강했다.
- 보강 후 단위 테스트 54개, JSON 검사, `git diff --check`가 통과했다.
  최종 전체 live dry-run에서는 NYT 99건, WSJ 98건과 ECB 15건을
  포함한 활성 소스 전체가 오류 없이 수집됐고, dry-run 상태 파일은
  변경되지 않았다.
- 두 검색 피드는 Google News가 발견한 결과를 기반으로 하므로 매체의
  모든 발행물을 수학적으로 보장하는 공식 API는 아니다. 다만 현재
  WSJ 공식 RSS의 최신성 결함을 피하면서, 8시간 수집 주기보다 긴 최근
  30일·최대 100건 창과 발행처 이중 검증을 적용한 운영 경로다.
- 1차 review에서 텔레그램 실패 후 재시도 누락, 광범위한 제목 제외어,
  설정만으로 다른 소스가 강제 예외를 사용할 수 있는 문제를 발견했다.
- 항목별 `last_notified_hash`를 기록해 현재 내용이 전달될 때까지
  `retry` 이벤트로 다시 보내도록 수정했다. 기존 상태 파일은 마지막
  알림 시점이 있으면 현재 hash를 알림 완료 hash로 안전하게 이관하고,
  bootstrap 기준선은 재시도하지 않는다.
- 제목 제외어는 ` - NYT Cooking`, `Print Edition`,
  `Foreign Exchange Rates - WSJ`처럼 제품 제목 형태로
  좁혔다. `South Korea's Recipe for Growth` 같은 정상 기사는 유지하는
  회귀 테스트를 추가했다.
- 강제 알림 허용 ID를 코드에서 `nyt_korea_news`, `wsj_korea_news`로
  고정하고, 설정에서 다른 소스에 플래그가 추가돼도 한국 언론 인용
  제외를 우회하지 못하도록 했다.
- 사용자 수정 지시에 따라 위 force-alert 예외 판단을 철회했다. 한국
  언론 재인용 제외를 가장 큰 원칙으로 확정하고, NYT·WSJ도 재인용이면
  제외하도록 되돌렸다. 두 매체의 `default_confidence=high`는 이 하드
  게이트를 통과한 검색 결과에만 적용된다.
- 개인 Chrome의 유료 로그인은 GitHub Actions에서 실행되는 수집기와
  세션을 공유할 수 없다. 쿠키 복사는 보안·만료·운영 독립성 문제로
  사용하지 않고, 텔레그램 원문 링크를 Chrome에서 열 때 기존 로그인을
  활용하는 방식으로 유지한다. 현재 Codex의 Chrome 연결도 활성화돼
  있지 않아 자동 브라우저 접근에는 별도 확장 연결이 필요하다.
- 사용자 수정 사항 반영 후 단위 테스트 56개와 최종 live dry-run을
  다시 통과했다. 최종 실행에서 NYT 100건, WSJ 98건을 검증했고 활성
  소스 전체가 오류 없이 끝났으며 `retry` 0건, 상태 파일 변경 0건이었다.
- 최종 review에서 Google News 기본 RSS에는 제목만 있어 유료 본문의
  한국 언론 인용을 직접 검사하지 못하는 문제를 발견했다. Google News
  링크를 원문으로 복원할 수는 있었지만 GitHub Actions와 같은 비로그인
  요청에서 NYT는 403, WSJ는 401을 반환했다.
- 개인 Chrome 세션을 서버로 복사하지 않고 원칙을 강화하기 위해,
  한국 언론사명과 `reported`, `according`, `citing`, `cited`가 함께
  색인된 별도 매체 제한 RSS를 제외 목록으로 사용한다. 기본 Korea
  피드와 동일 GUID인 기사는 버리고, 제외 피드 자체가 실패하면 해당
  매체 수집 전체를 fail-closed 처리하도록 구현했다.
- 재리뷰의 live 검증에서 긴 OR 제외 검색이 `site:`와 `when:` 제약을
  무시하고 한국 언론사 자체 기사만 반환해 실질적인 제외 0건이 되는
  문제를 발견했다. 이를 한국 언론사별 짧은 검색 12개로 분리하고,
  응답 항목이 있는데 NYT·WSJ 발행처 검증 결과가 0건이면 semantic
  drift로 간주해 fail-closed 처리하도록 보강했다.
- 제외 검색 언론사명은 본문 필터의 `excluded_attribution_rules`에 모두
  존재해야 한다는 설정 회귀 테스트를 추가했다. 최종 단위 테스트
  59개가 통과했고, live 대상 소스 점검에서 NYT 99건과 WSJ 98건을
  정상 수집했다. 이전 100건 대비 NYT 1건은 동일 GUID 제외가 실제로
  적용된 결과다.
- 전체 live dry-run 재검증은 기존 WTO 피드의 HTTP 520 때문에 두 번
  중단됐다. 이번 변경 대상인 NYT·WSJ의 단독 live 수집은 성공했으며,
  WTO 장애는 코드 오류와 분리해 운영 시점에 다시 확인한다.
- 최종 review는 분할 제외 검색, 발행처 0건 semantic fail-closed,
  GUID 합집합, 알림 재시도, 설정·본문 필터 동기화 및 문서 기록을
  재검증하고 P1/P2 문제가 없다고 승인했다.
- 구현 커밋 `1de4cd7`을 최신 `origin/main` 기준의
  `agent/expand-global-sources` 브랜치에 푸시했다. 기존 초안 PR #1의
  제목·설명·검증 내역도 NYT·WSJ 우선순위와 최상위 재인용 제외 원칙에
  맞게 갱신했다.

#### 현재 단계

- [x] 최신 main 동기화와 기존 PR 상태 확인
- [x] constitution 에이전트 예외 우선순위 검토
- [x] NYT·WSJ 수집 경로와 최신성 비교
- [x] 고우선 소스·발행처 검증·0건 오류 처리 구현
- [x] 단위 테스트와 live dry-run
- [x] review 에이전트 검토
- [x] 커밋, 푸시, PR 갱신

## 변경 이력 작성 규칙

작업 방법, 범위 변경, 소스 채택·보류 이유, 검증 결과, 운영 반영 결과를
날짜별로 이 문서에 계속 추가한다. 최초 기획 의도는 삭제하거나 의미를
바꾸지 않는다.
